<Project>

  <PropertyGroup Label="SDK Versions">
    <LangVersion>latest</LangVersion>
  </PropertyGroup>

  <PropertyGroup Label="Reproducible build">
    <Deterministic>true</Deterministic>
    <!-- Normalizes paths inside .pdb/.dll on CI for reproducible output -->
    <ContinuousIntegrationBuild Condition="'$(GITHUB_ACTIONS)' == 'true'">true</ContinuousIntegrationBuild>
  </PropertyGroup>

  <PropertyGroup Label="Package information">
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageProjectUrl>https://github.com/groupdocs-watermark/GroupDocs.Watermark.Mcp</PackageProjectUrl>
    <RepositoryUrl>https://github.com/groupdocs-watermark/GroupDocs.Watermark.Mcp</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <Authors>GroupDocs Contributors</Authors>
    <Company>Aspose</Company>
    <Copyright>Copyright © Aspose Pty Ltd $([System.DateTime]::Now.Year)</Copyright>
    <PublishRepositoryUrl>true</PublishRepositoryUrl>
    <EmbedUntrackedSources>true</EmbedUntrackedSources>
    <AllowedOutputExtensionsInPackageBuildOutputFolder>true</AllowedOutputExtensionsInPackageBuildOutputFolder>
  </PropertyGroup>

  <PropertyGroup Label="Package Versions">
    <GroupDocsWatermarkMcp>26.7.1</GroupDocsWatermarkMcp>
  </PropertyGroup>

  <PropertyGroup Label="External Dependency Versions">
    <GroupDocsMcpCore>26.4.1</GroupDocsMcpCore>
    <GroupDocsWatermark>26.6.0</GroupDocsWatermark>
    <MicrosoftExtensionsHosting>10.0.5</MicrosoftExtensionsHosting>
    <ModelContextProtocol>1.1.0</ModelContextProtocol>
    <MicrosoftSourceLinkGithub>8.0.0</MicrosoftSourceLinkGithub>
    <!-- SkiaSharp pin removed at 26.6.0: the GroupDocs.Watermark.Net100 runtime
         sub-package (resolved under net10.0) now declares SkiaSharp 3.119.0 +
         SkiaSharp.NativeAssets.Linux.NoDependencies transitively, so the manual
         csproj ref is redundant — libSkiaSharp.so ships via transitive resolution.
         See pitfall S in docs/mcp-update-agent.md. -->
  </PropertyGroup>

  <PropertyGroup Label="NuGet Common Settings">
    <PackageReleaseNotes>https://github.com/groupdocs-watermark/GroupDocs.Watermark.Mcp/releases/tag/$(GroupDocsWatermarkMcp)</PackageReleaseNotes>
  </PropertyGroup>

</Project>
