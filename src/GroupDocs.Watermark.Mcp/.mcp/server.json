{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-10-17/server.schema.json",
  "description": "MCP server for GroupDocs.Watermark — add and search document watermarks via AI agents.",
  "name": "io.github.groupdocs-watermark/groupdocs-watermark-mcp",
  "version": "26.9.0",
  "packages": [
    {
      "registryType": "nuget",
      "registryBaseUrl": "https://api.nuget.org/v3/index.json",
      "identifier": "GroupDocs.Watermark.Mcp",
      "version": "26.9.0",
      "transport": {
        "type": "stdio"
      },
      "packageArguments": [],
      "environmentVariables": [
        {
          "name": "GROUPDOCS_MCP_STORAGE_PATH",
          "value": "{storage_path}",
          "variables": {
            "storage_path": {
              "description": "Base folder for input and output files. Defaults to the current working directory.",
              "isRequired": false,
              "isSecret": false
            }
          }
        },
        {
          "name": "GROUPDOCS_MCP_OUTPUT_PATH",
          "value": "{output_path}",
          "variables": {
            "output_path": {
              "description": "Optional — separate folder for output files. Defaults to GROUPDOCS_MCP_STORAGE_PATH.",
              "isRequired": false,
              "isSecret": false
            }
          }
        },
        {
          "name": "GROUPDOCS_LICENSE_PATH",
          "value": "{license_path}",
          "variables": {
            "license_path": {
              "description": "Path to the GroupDocs.Total.lic license file. Omit to run in evaluation mode.",
              "isRequired": false,
              "isSecret": false
            }
          }
        },
        {
          "name": "GROUPDOCS_METERED_PUBLIC_KEY",
          "value": "{metered_public_key}",
          "variables": {
            "metered_public_key": {
              "description": "Metered (pay-per-use) public key. Requires GROUPDOCS_METERED_PRIVATE_KEY as well. Takes precedence over GROUPDOCS_LICENSE_PATH. Metered mode reports usage to GroupDocs servers, so it needs outbound connectivity.",
              "isRequired": false,
              "isSecret": false
            }
          }
        },
        {
          "name": "GROUPDOCS_METERED_PRIVATE_KEY",
          "value": "{metered_private_key}",
          "variables": {
            "metered_private_key": {
              "description": "Metered (pay-per-use) private key. Treat as a secret: supply it via an environment variable or a secret store, never hardcoded in a committed MCP client config.",
              "isRequired": false,
              "isSecret": true
            }
          }
        }
      ]
    }
  ],
  "repository": {
    "url": "https://github.com/groupdocs-watermark/GroupDocs.Watermark.Mcp",
    "source": "github"
  }
}
